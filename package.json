{
  "name": "finder-tube",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch src/style/sass/main.sass src/style/css/main.css",
    "tsc": "tsc -w", 
    "server": "lite-server --baseDir='src'",
    "dev": "concurrently \"npm run sass\" \"npm run tsc\" \"npm run server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1",
    "sass": "^1.42.1"
  },
  "dependencies": {
    "bulma": "^0.9.3",
    "concurrently": "^6.2.2"
  }
}
